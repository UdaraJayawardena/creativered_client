export class ComplainType {
    complainType: string;
    id: number;

    constructor(complainType: string, id: number) {
        this.complainType = complainType;
        this.id = id;
    }
}
